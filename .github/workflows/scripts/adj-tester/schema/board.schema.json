{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Board configuration schema",
    "description": "Main board configuration including ID, IP address, and references to measurements and packets files.",
    "type": "object",
    "additionalProperties": true,
    "required": [
        "board_id",
        "board_ip",
        "measurements",
        "packets"
    ],
    "properties": {
        "board_id": {
            "type": "integer",
            "minimum": 0,
            "maximum": 4294967295,
            "description": "Unique 32-bit unsigned integer identifier for the board"
        },
        "board_ip": {
            "type": "string",
            "format": "ipv4",
            "description": "IP address string for network communication"
        },
        "measurements": {
            "type": "array",
            "minItems": 1,
            "items": {
                "type": "string",
                "pattern": "^[^\\\\/:*?\"<>|]+\\.json$"
            },
            "description": "Array of measurement file paths relative to board directory"
        },
        "packets": {
            "type": "array",
            "minItems": 1,
            "items": {
                "type": "string",
                "pattern": "^[^\\\\/:*?\"<>|]+\\.json$"
            },
            "description": "Array of packet file paths relative to board directory"
        }
    }
}